# Intentionally empty. The official solver base image already includes
# minotaur_subnet and common blockchain dependencies. This solver uses only
# stdlib code for ABI encoding and optional JSON-RPC calls.
